{
    "name": "get_github_data",
    "description": "Retrieves data from the API using id as filter if needed. This function is called only if the data has not been previously retrieved or need to be retrived again.",
    "parameters": {
      "type": "object",
      "properties": {
        "github_id": {
          "type": "string",
          "description": "The GitHub ID for which the data needs to be fetched"
        }
      },
      "optional": [
        "github_id"
      ]
    }
  }